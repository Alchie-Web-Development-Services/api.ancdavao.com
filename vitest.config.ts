import {defineConfig} from "vitest/config";

export default defineConfig({
  test: {
    // You can add Vitest configuration options here
    // For example, to enable globals:
    // globals: true,
  },
});
